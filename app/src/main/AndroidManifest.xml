<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- allowBackup=false: Room holds history and per-domain cookies; neither belongs in cloud backup -->
    <application
        android:name=".FyiApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.FyiPlayer"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- "Share with" from another app: raw link text, often with surrounding prose;
                 MainActivity pulls the first https URL out of EXTRA_TEXT. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Open-with/"share link" for the hosts the resolver chain can play: YouTube goes
                 tier0 (NewPipeExtractor), everything else falls through to tier1 (yt-dlp engine);
                 same generic pageUrl path, no per-host wiring needed beyond this filter. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="youtube.com" />
                <data android:scheme="https" android:host="www.youtube.com" />
                <data android:scheme="https" android:host="m.youtube.com" />
                <data android:scheme="https" android:host="youtu.be" />
                <data android:scheme="https" android:host="facebook.com" />
                <data android:scheme="https" android:host="www.facebook.com" />
                <data android:scheme="https" android:host="m.facebook.com" />
                <data android:scheme="https" android:host="fb.watch" />
                <data android:scheme="https" android:host="twitter.com" />
                <data android:scheme="https" android:host="x.com" />
                <data android:scheme="https" android:host="tiktok.com" />
                <data android:scheme="https" android:host="www.tiktok.com" />
                <data android:scheme="https" android:host="m.tiktok.com" />
                <data android:scheme="https" android:host="vm.tiktok.com" />
                <data android:scheme="https" android:host="vt.tiktok.com" />
            </intent-filter>
        </activity>

        <!-- Hosts Google's real sign-in page for first-party YouTube login; launched only from
             within the app (AccountSettings), never from outside it. -->
        <activity
            android:name=".YoutubeLoginActivity"
            android:exported="false" />

        <!-- services are declared by the phase that adds them: playback (mediaPlayback),
             downloads (dataSync). A declaration without its class is a runtime crash. -->
        <!-- exported=true is required here, not a hole: media button routing (headset/Bluetooth,
             Assistant, the system media session) binds to this service via the
             androidx.media3.session.MediaSessionService action, which needs a cross-process bind. -->
        <service
            android:name=".player.PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <service
            android:name=".download.DownloadService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- A finished download can only be handed to another app as a content:// Uri;
             ACTION_VIEW on a file:// Uri throws FileUriExposedException on this targetSdk. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
